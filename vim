Git init
git add *
git remote add origin https://github.com/muhbader/howhow.git

git commit (-m for message)
Git push <url>
git push -u origin master

Git checkout == for changing branches
git checkout -b <bracnhname>

Git clone https
Git restore <path> == to restore the files to the computer in a specific path

for insert I
For exit: esc : w q
Test tse